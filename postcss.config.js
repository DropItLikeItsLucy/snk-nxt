// postcss.config.js (Ensure this is the content)
module.exports = {
  plugins: {
    tailwindcss: {}, // Tells PostCSS to use Tailwind
    autoprefixer: {}, // Adds vendor prefixes for browser compatibility
  },
}